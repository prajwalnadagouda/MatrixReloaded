networks:
  peercmpe273:
    driver: bridge
    ipam:
      config:
      - subnet: 172.59.0.0/16
      driver: default
services:
  app:
    build: ./app
    networks:
    - peercmpe273
    ports:
    - 38359:5000
    - 39774:2008
    volumes:
    - ./info.ini:/app/info.ini
version: '2.2'
